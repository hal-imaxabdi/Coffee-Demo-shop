{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (HTML file)",
            "request": "launch",
            "type": "pwa-chrome",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}

